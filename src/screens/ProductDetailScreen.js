import React, { useState, useRef, useEffect } from 'react';
import { View, Text, Image, TouchableOpacity, StyleSheet, ScrollView, FlatList, SafeAreaView, Platform, Share, Modal, TextInput, KeyboardAvoidingView, TouchableWithoutFeedback, Keyboard, Animated, Dimensions, useColorScheme, } from 'react-native';
import Svg, { Path, G } from 'react-native-svg';
import { LinearGradient } from 'expo-linear-gradient';

export default function ProductDetailScreen() {
  return (
    <SafeAreaView>
      <Text>Product detail placeholder</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});