import React, { useState, useRef } from 'react';
import { View, Text, TouchableOpacity, StyleSheet, ScrollView, Image, SafeAreaView, useColorScheme, Linking, Dimensions, Platform, StatusBar, Modal, Animated, RefreshControl, } from 'react-native';
import Svg, { Path, G, Circle } from 'react-native-svg';
import { LinearGradient } from 'expo-linear-gradient';
import { BlurView } from 'expo-blur';

export default function ProfileScreen({ route, navigation }) {
  return (
    <SafeAreaView>
      <Text>Profile screen placeholder</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});