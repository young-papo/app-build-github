import React, { useState, useRef, useEffect } from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet, FlatList, Image, SafeAreaView, Animated, ScrollView, Modal, Platform, Keyboard, useColorScheme, } from 'react-native';
import Svg, { Path, G } from 'react-native-svg';

export default function MessagesScreen() {
  return (
    <SafeAreaView>
      <Text>Messages screen placeholder</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});